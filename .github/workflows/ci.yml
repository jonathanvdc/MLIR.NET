name: CI

on:
  push:
    branches:
      - main
      - master
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore
        run: dotnet restore MLIR.NET.slnx

      - name: Build
        run: dotnet build MLIR.NET.slnx --configuration Release --no-restore

      - name: Test
        run: dotnet test MLIR.NET.slnx --configuration Release --no-build
