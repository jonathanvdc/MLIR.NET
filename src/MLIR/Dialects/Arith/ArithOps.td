//===- ArithOps.td - arith dialect op definitions -----------*- tablegen -*-===//
//
// This file mirrors the upstream MLIR arith dialect entry point so the runtime
// project can consume the same generated definitions as downstream dialect projects.
//
//===----------------------------------------------------------------------===//

include "mlir/Dialect/Arith/IR/ArithOps.td"
