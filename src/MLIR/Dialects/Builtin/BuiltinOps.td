//===- BuiltinOps.td - builtin dialect op definitions -------*- tablegen -*-===//
//
// This file mirrors the upstream MLIR builtin dialect entry point so the runtime
// project can consume the same generated definitions as downstream dialect projects.
//
//===----------------------------------------------------------------------===//

include "mlir/IR/BuiltinOps.td"
