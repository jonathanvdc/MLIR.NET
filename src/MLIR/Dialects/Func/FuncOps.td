//===- FuncOps.td - func dialect op definitions ------------*- tablegen -*-===//
//
// This file mirrors the upstream MLIR func dialect entry point so the runtime
// project can consume the same generated definitions as downstream dialect projects.
//
//===----------------------------------------------------------------------===//

include "mlir/Dialect/Func/IR/FuncOps.td"
